1.Info table을 만들면서 Tag를 만들 시 info_id를 조회하는 과정을 넣어야 할까? (해결)
    create 후 return된 객체에 id값이 있음

2.tag를 여러개를 한번에 만드는데, create는 어쩔 수 없이 여러번 호출해야 할까?
2-1. then을 안쓰면 처리과정에 문제가 생길 것 같은데, 실제로도 문제가 생길까?

3. Tag를 프론트에서 id로 받나 name으로 받나? 일단 Id로 생각하고 받았는데, 이렇게 되면 굳이 tag테이블을 만들 필요가 없을 것 같다.
    왜냐하면 어짜피 프론트에서 id-name 값들을 알고 있고, 그 해당하는 값에 따라 처리를 하는 것이니까.
    그렇다고 name으로 받으면 연산 수가 많아질 것이다. tag는 기본적으로 이름이 다 다르고 그에 따라 여러개가 들어올수록 여러번 select 해야하니까

4. 중복적으로 쓰이는 코드를 분리

5. 토큰에서 보내느 계정의 값에 따라 코드가 달라짐
